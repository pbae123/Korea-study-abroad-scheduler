# Copy this file to backend/.env. Never commit backend/.env or any real key.
# Generate a value with: python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=replace-with-a-long-random-secret
DJANGO_DEBUG=true
FRONTEND_ORIGIN=http://localhost:5173
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5vl:7b

# Optional. Keep this server-side only; never use a VITE_HF_TOKEN variable.
# HF_TOKEN=
