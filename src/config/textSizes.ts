// Per-region text sizes (rem). Edit any value to resize just that part of the UI.
export const TEXT_SIZES = {
  scheduleTabLabel: 0.75, // "Option 1" tab labels
  yAxisTimeLabel: 0.7git, // grid y-axis time labels, e.g. "(1) 09:00-09:50"
  dayOfWeekHeader: 0.75, // grid column headers, e.g. "Mon"
  classBlockTitle: 0.6875, // class name inside a placed block
  classBlockDetail: 0.6875, // time/location/conflict text inside a placed block
}
